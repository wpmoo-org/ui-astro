---
interface Props {
  novalidate?: boolean;
  class?: string;
  [key: string]: unknown;
}

const { novalidate = false, class: className = "", ...rest } = Astro.props;
---

<form {...rest} class:list={["field-form", className]} novalidate={novalidate}><slot /></form>
