---
interface Props {
  text: string;
  class?: string;
  [key: string]: unknown;
}

const { text, class: className = "", ...rest } = Astro.props;
---

<kbd {...rest} class={className || undefined}><slot>{text}</slot></kbd>
